package funhacks.curry29.morauknow;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class HomeActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_home);

        /*
        ImageButton eventlist_button = (ImageButton) this.findViewById(R.id.imageButton);
        eventlist_button.setImageResource(R.drawable.sample);


        ImageButton areamap_button = (ImageButton) this.findViewById(R.id.imageButton2);
        areamap_button.setImageResource(R.drawable.sample);
        */
    }
}
